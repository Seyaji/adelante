import { DataTypes } from '../types';
const dataTypes: DataTypes = {
    address: 'string',
    bool: 'boolean',
    uint: 'number',
    uint8: 'number',
    uint16: 'number',
    uint32: 'number',
    uint64: 'number',
    uint128: 'number',
    uint256: 'number',
}
export default dataTypes 