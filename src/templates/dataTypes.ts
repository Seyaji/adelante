import { DataTypes } from '../types.js'

const dataTypes: DataTypes = {
    address: 'string',
    bool: 'boolean',
    unit: 'number',
    unit8: 'number',
    unit16: 'number',
    unit32: 'number',
    unit64: 'number',
    unit128: 'number',
    uint256: 'number',
}
export default dataTypes